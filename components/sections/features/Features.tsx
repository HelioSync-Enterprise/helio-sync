export default function Features(){
    return (
        <section className="relative bg-helio-bg-secondary py-16">
            <div className="absolute inset-0 opacity-30" style={{ background: "radial-gradient(ellipse at center top, var(--helio-bg-accent) 0%, transparent 60%)" }} />
            <h1 className="text-4xl font-bold text-center mb-12"
            >Por que escolher Helio<span className="text-gradient-gold">Sync</span>?</h1>
            <p className="max-w-2xl mx-auto text-center text-lg text-secondary mb-8"
            >Apresentamos uma forma viável e eficaz de gerar energia diretamente da sua residência.</p>
        </section>
    )
}